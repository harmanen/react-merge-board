import React, { ReactNode } from 'react';
import { Box } from '@mui/material';
import styles from '../page.module.css';

export function InfoBox() {
  return <Box className={styles.infoContainer}>INFO</Box>;
}
